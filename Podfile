# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'CovidTracker' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CovidTracker
pod 'YOChartImageKit', '~> 1.1'
end

target 'CovidTracker WatchKit App' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CovidTracker WatchKit App
  platform :watchos, '6.0'
pod 'YOChartImageKit', '~> 1.1'
end

target 'CovidTracker WatchKit Extension' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CovidTracker WatchKit Extension
  platform :watchos, '6.0'
pod 'YOChartImageKit', '~> 1.1'
end
